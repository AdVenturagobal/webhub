{
  "name": "webhub-static-optimize",
  "version": "1.0.0",
  "description": "WebHub static site asset optimization (CSS/JS merge & minify)",
  "scripts": {
    "build": "gulp build"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-clean-css": "^4.3.0",
    "gulp-uglify": "^3.0.2",
    "gulp-rename": "^2.0.0"
  }
} 